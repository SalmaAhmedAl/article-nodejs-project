<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Welcome</title>
  </head>
  <body>
    <h1>Welcome, <%= name %>!</h1>
    <% if (age < 20) { %>
     <p>You are yong!</p>
    <% } else { %>
     <p>You are old!</p>
    <% } %>
  </body>
</html>
